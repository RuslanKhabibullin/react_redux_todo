My first attempt to realize React-Redux application (with create-react-app). Simple Todo application.

## Setup

Run `npm i` to install all project dependencies

## Development

Run `npm start` - start local development server 
